<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ServerMonitor.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ServerMonitor"
    xmlns:vm="clr-namespace:ServerMonitor.ViewModels"
    Shell.FlyoutBehavior="Flyout"
    Shell.BackgroundColor="LightGray"
    Shell.FlyoutBackgroundColor="White"
    Shell.FlyoutWidth="250"
    Shell.ForegroundColor="Black">

    <Shell.Resources>
        <ResourceDictionary>
            <Style Class="FlyoutItemLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="White" />
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Barra de título personalizada con logo centrado, ícono de hamburguesa nativo -->
    <Shell.TitleView>
        <Grid ColumnDefinitions="Auto,*,Auto" VerticalOptions="Center">
            <Label Grid.Column="0" WidthRequest="40"/>
            <!-- Placeholder para el ícono de hamburguesa -->
            <Image Grid.Column="1"
                   Source="logo.png"
                   HeightRequest="50"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"/>
            <Label Grid.Column="2" WidthRequest="40"/>
        </Grid>
    </Shell.TitleView>
    <!-- Opciones del menú con estilo mejorado -->
    <FlyoutItem Title="DATOS" Icon="{FontImageSource FontFamily=MaterialIcons, Glyph=home, Color=White}" FlyoutDisplayOptions="AsSingleItem">
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate local:MainPage}"/>
    </FlyoutItem>
    <FlyoutItem Title="GRÁFICOS" Icon="{FontImageSource FontFamily=MaterialIcons, Glyph=bar_chart, Color=White}" FlyoutDisplayOptions="AsSingleItem">
        <ShellContent Route="GraphicsPage" ContentTemplate="{DataTemplate local:GraphicsPage}"/>
    </FlyoutItem>
    <!-- Estilo del flyout -->
    <Shell.FlyoutHeader>
        <Grid BackgroundColor="#4d62da" Padding="20" HeightRequest="100">
            <Image Source="logo_menu.png" HeightRequest="60" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Grid>
    </Shell.FlyoutHeader>
    <Shell.FlyoutFooter>
        <Grid BackgroundColor="#4d62da" Padding="20" HeightRequest="50">
            <Label Text="DieFer v1.0 - 2025" FontSize="12" HorizontalOptions="Center" VerticalOptions="Center" Margin="-10" TextColor="White"/>
        </Grid>
    </Shell.FlyoutFooter>
</Shell>